Placeholder so the Dockerfile glob `COPY texlive-source* ...` always matches.
Drop a TeX Live source tree beside this file as `texlive-source/` and the build
uses it instead of fetching the pinned commit. See ../RELINK.md.
